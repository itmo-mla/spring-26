# Лабораторная работа №4

## Задание

1. Выбрать датасет для восстановления плотности распределения, например, на [kaggle](https://www.kaggle.com/);
2. Реализовать GMM;
3. Обучить модель на выбранном датасете;
4. Оценить качество модели через ПМП;
5. Сравнить результаты с эталонной реализацией из библиотеки scikit-learn:
   - точность модели;
6. Подготовить отчет, включающий:
   - описание датасета;
   - результаты экспериментов;
   - сравнение с эталонной реализацией;
   - выводы.

## Отчёт

### 2. Датасет

В качестве датасета выбран [Iris Dataset](https://scikit-learn.org/stable/auto_examples/datasets/plot_iris_dataset.html).



#### Подготовка данных:

- Перед обучением применена стандартизация (z-score нормализация).

```python
from sklearn.datasets import load_iris
import numpy as np

iris = load_iris()
X, y = iris.data, iris.target

mean, std = X.mean(axis=0), X.std(axis=0)
X_scaled = (X - mean) / std
```

### 3. Программная реализация

Структура проекта:

```
lab4/
├── source/
│   ├── gmm.py       # Реализация GMM
│   └── lab4.ipynb   # Эксперименты
└── README.md
```

В программе реализовано согласно заданию:

* **E-шаг** — вычисление G через многомерное нормальное распределение — [код](source/gmm.py)

* **M-шаг** — обновление весов, средних и ковариационных матриц — [код](source/gmm.py)

* **Регуляризация ковариаций** — добавление `1e-6 · I` для численной устойчивости — [код](source/gmm.py)

* **Критерий остановки** — по изменению логарифмического правдоподобия — [код](source/gmm.py)

### 4. Анализ и сравнение

В рамках эксперимента были рассмотрены:
- обучение GMM при `k=3` и анализ выученных компонент;
- оценка качества кластеризации через ARI и Silhouette Score;
- сравнение с реализацией из `sklearn`.

#### Обучение GMM (k=3)

Выученные компоненты (средние в исходном пространстве признаков):

```
Компонента   Вес (π_j)   Среднее [sepal_l, sepal_w, petal_l, petal_w]
──────────────────────────────────────────────────────────────────────
0            0.2993      [5.915  2.778  4.202  1.297]   ← versicolor
1            0.3333      [5.006  3.428  1.462  0.246]   ← setosa
2            0.3673      [6.545  2.949  5.480  1.985]   ← virginica
```

Истинные средние по классам:

```
setosa:      [5.006  3.428  1.462  0.246]
versicolor:  [5.936  2.770  4.260  1.326]
virginica:   [6.588  2.974  5.552  2.026]
```

Модель корректно выделила три компоненты, соответствующие видам ириса. Средние выученных компонент близки к истинным, что говорит о том, что GMM успешно восстановил плотность распределения данных.

#### Оценка качества

Метрики при `k=3`:

```
Adjusted Rand Index (ARI):  0.9039
Silhouette Score:           0.3742
Log-Likelihood:           -290.53
```

Распределение объектов по кластерам:

```
Класс          Кластер 0    Кластер 1    Кластер 2
──────────────────────────────────────────────────
setosa         0            50           0
versicolor     45           0            5
virginica      0            0            50
```


#### Сравнение с эталоном

```python
# моя реализация
GMM(k=3, max_iter=100, tol=1e-4)

# sklearn
GaussianMixture(n_components=3, max_iter=100, tol=1e-4, random_state=42)
```

```
Метрика                   самописный GMM       sklearn GMM         
Log-Likelihood            -290.53              -299.89             
ARI                       0.9039               0.5608              
Silhouette Score          0.3742               0.3139   
```

GMM показал значительно лучший ARI (`0.9039` против `0.5608`).

Sklearn завершил работу за `21` итераций EM; моя реализация — за `28` итераций 

## Вывод

В ходе лабораторной работы реализована модель Gaussian Mixture Model с обучением через алгоритм EM на датасете Iris.

